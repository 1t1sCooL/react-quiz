apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-quiz-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: react-quiz-app
  template:
    metadata:
      labels:
        app: react-quiz-app
    spec:
      containers:
        - name: react-quiz-app
          image: react-quiz-app:latest
          ports:
            - containerPort: 80
          env:
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
